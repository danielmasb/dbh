# DBH – Personal Agent Hub

This repository contains personal agents managed under the DBH (Daniel's Bot Hub) project.

## Structure

```
agents/          # Individual agent directories
  knuspr/        # Knuspr grocery agent
    CLAUDE.md    # Agent-specific instructions
    config.json  # Agent configuration
    favourites.json  # Saved favourite items
```

## Environment

Copy `.env.example` to `.env` and fill in required values before running any agent.
